# Server dependencies
fastapi==0.115.0
uvicorn[standard]==0.30.1
httpx==0.27.0
supabase==2.10.0

# Legacy PostgreSQL (keep for now, can remove later)
psycopg[binary]==3.2.3

# Worker dependencies  
requests==2.32.3
websocket-client==1.8.0
